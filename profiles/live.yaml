# Conservative live / larger account
label: "Live Conservative"
description: "Performance plan gates, no micro overlay, conservative risk"
mode: live
practice:
  micro:
    enabled: false
  growth:
    enabled: false
mt5:
  account_mode: real
execution:
  live_trading_enabled: true
  mt5_trading_enabled: true
  # Phase 0 safety: this profile explicitly opts into live order routing.
  explicit_opt_in_danger_zone: true