"""Strategy classes (custom Python strategies beyond the in-config setups).

Currently implemented:
    - bankbot: MA-crossover strategy (PineScript port) with 12 MA variants
      and auto-tuning support. See strategies/bankbot.py.
"""
