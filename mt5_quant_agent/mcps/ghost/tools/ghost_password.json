{
  "name": "ghost_password",
  "description": "Reset the password for a database's default user (tsdbadmin). This changes the password on the server itself. This operation is irreversible. Existing connections using the old password will fail to reconnect.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "name_or_id": {
        "type": "string",
        "description": "Database name or identifier"
      },
      "password": {
        "type": "string",
        "description": "The new password. If not provided, a secure password will be automatically generated"
      }
    },
    "required": [
      "name_or_id"
    ],
    "additionalProperties": false
  }
}